# DealPilot — External Review Disposition (2026-07-06)
Status: Disposition complete; implementation follow-ups open (tracked in §5) · Owner: CEO/CTO · Source: external agent review, overall score 58/100, "would not invest at $16M post on docs alone."

Method: every finding was **verified against the repo before disposition** (the reviewer gets the same treatment our memos give sellers). Dispositions: **ACCEPTED** (doc changed this pass), **PARTIAL** (part accepted, part rejected with rationale), **ALREADY-TRACKED** (finding restates an existing gate/risk — cited), **REJECTED** (with a falsifiable revisit trigger where honest).

## 0. Verification of the review's own evidence

| Reviewer claim | Repo check | Result |
|---|---|---|
| Product emits "Pursue / Pass / Cannot evaluate" verdicts | `../product/prd.md` FR-6 said exactly this (session-1 doc), directly contradicting `../legal/compliance.md` §6.1 ("no buy/sell/pass outputs") written later | **SUBSTANTIATED — real cross-doc contradiction, the review's best catch** |
| Marketing claims "hallucination-free" / "structurally incapable of making it up" | `executive-summary.md` one-liner + elevator pitch, `marketing.md` trust pillar | **SUBSTANTIATED** |
| "Chat too early" | Chat appears only as non-goal #8 in `../product/prd.md` ("chat is an accessory, P2") | Overstated — already a non-goal |
| Seasonality, credit-confusion, reviewer-cost risks missing | Register rows 6, 19, 53 | Already tracked |

Reviewer accuracy verdict: high on the substantive catches, mild overreach on two. Findings graded accordingly below.

## 1. Dispositions

**RR-1 — Evidence-source dependency (reviewer P0).** ALREADY-TRACKED (R-2, top of register; MVP already one-metro; RB-03 outage runbook + drills exist) **+ ACCEPTED sharpener**: fallback map per blocking claim type is now an MVP requirement — no blocking type may depend on a single source without a documented manual-evidence path (`../roadmap/mvp.md` §2).

**RR-2 — "Users pay for refusals" unproven (P0).** ALREADY-TRACKED — this is verbatim the MVP kill criterion (`../roadmap/mvp.md` §6, hypothesis H1). **+ ACCEPTED instrumentation**: export/forward behavior added as a success gate (≥25% of active partners share a memo) — sharing refusal-heavy output is the strongest willingness-to-pay tell available pre-scale.

**RR-3 — Architecture overbuilt for seed (P0).** **ACCEPTED in substance.** New `../architecture/system-design.md` §10 defines the MVP profile (modular monolith + Temporal Cloud + Postgres-for-everything + S3 + Stripe) with measured graduation triggers for Kafka, OpenSearch, mesh, OPA, and service splits. Two pushbacks: **Temporal stays** (the workflow genuinely is long-running/retried/human-signaled; a hand-rolled job runner is the code we'd write worst) and **EKS-lite stays** (single small cluster; an ECS→EKS migration later costs more than thin EKS now). The reviewer's core point — the docs read as build-it-all-now — was correct and is now structurally false.

**RR-4 — Milestone stacking on $4M (P0).** PARTIAL. The gate ladder already sequences (mvp → v1 → enterprise; SOC 2 II and API GA sit in the enterprise tranche behind revenue proof, `../roadmap/enterprise.md` §2). **ACCEPTED**: the SMB wedge is now metric-gated, not calendar-gated (`../roadmap/v1.md`), and the deep-bear case (RR-11) documents what breaks if stacking fails. Seed-gate breadth itself is retained deliberately: those are Series-A market-clearing gates, and the downshift trigger is the pre-committed answer to missing them — the honest alternative to narrower gates is a smaller company, which is exactly what §4.3 produces on a miss.

**RR-5 — COGS credibility (P0).** PARTIAL. Per-claim-class and per-source cost instrumentation from day one: **ACCEPTED** (`../architecture/observability.md` §7 now explicit). Replacing credits with claim-class pricing: **REJECTED** — the reviewer's own risk #17 (credit confusion) argues against a more complex pricing surface; instead, credits-per-deal-type get **calibrated from measured distributions** (same data, simpler buyer contract).

**RR-6 — Regulatory perimeter fragile (P0).** **ACCEPTED — the critical finding.** The PRD verdict band was a genuine session-to-session contradiction with the compliance perimeter. Fixed across five docs: `../product/prd.md` FR-6 now specifies an **evidence band** (Substantiated / Substantiated with open items / Material contradictions / Cannot evaluate — describes evidence state, never action); Dana's user story reworded; `../product/ux.md` verdict language → evidence-band language; the exec-summary/pitch tagline "decides whether they're worth buying" → "finds out what's actually true about them" (perimeter-safe, still has teeth). Counsel review of every output template was already policy (`../legal/compliance.md` §6); the templates now match the policy.

**RR-7 — Evidence-Graph moat vs privacy limits (P1).** PARTIAL/ALREADY-SPECIFIED. The reusable-asset boundary already exists precisely (`../architecture/ai-architecture.md` §8: public-facts-only global cache; `../legal/privacy.md` §3/§9: opt-in for anything trained). **ACCEPTED action** (contract-side, not doc-side): the four reusable classes — public facts, source-reliability metadata, anonymized aggregates, consented outcome data — get named in the DPA template at next counsel pass so the moat story and the data-rights story are the same story in writing.

**RR-8 — "VERIFIED is a dangerous word" (P0).** PARTIAL. Renaming to SOURCE-SUPPORTED: **REJECTED for now** — the vocabulary is load-bearing across product, API enums (`../engineering/api.md` §9 commits status semantics within a major version), and brand; and the ToS already defines VERIFIED as evidence-support-at-a-time, not truth. **Falsifiable revisit trigger** (so this is a decision, not a dodge): materiality-weighted — (a) **any** false VERIFIED on a *blocking* claim in the external red-team → RCA + launch-gate review; (b) false VERIFIED on non-blocking claims > 0.5% → mitigation plan; (c) aggregate > 1%, or a VI-class incident rooted in status misreading → rename RFC within a week. **ACCEPTED now**: user-facing VERIFIED always carries the evidence as-of date (`../architecture/ai-architecture.md` §2.2), and "hallucination-free"/absolute-accuracy claims are banned marketing vocabulary (`../business/marketing.md`) — the overclaim was real and is gone.

**RR-9 — Chat/API/MCP too early.** PARTIAL. Chat: already a non-goal (reviewer overstated). API alpha **stays** — design-partner-keys-only, and the Phase-3 API thesis needs spec feedback before GA freeze. MCP: **ACCEPTED, deferred to post-Series-A** (`../roadmap/v1.md`) — distribution upside doesn't beat launch-quarter focus.

**RR-10 — Corpus provenance unproven; external red-team required.** **ACCEPTED fully.** New MVP acceptance gate 7: independent reviewer, 50 real deals, **raw outputs**, report to the data room unedited (`../roadmap/mvp.md` §4). New `../architecture/ai-architecture.md` §13.6: corpus datasheets under external review, annual red-team, raw-output reporting policy. This was the review's second-best catch — internal evals were grading our own homework with our own key.

**RR-11 — Model a harsher case.** **ACCEPTED.** Deep-bear row added (`../business/financial-model.md` §8.1): activation −40%, source fees ×2, enterprise cycle ×2, compression 50% slower → FY3 ≈ $5.2M ARR; the company survives **only** via the automatic mo-18 downshift (cash-out ≈ mo 29 vs mo 24 without it). The stress test's real output is confirmation that the downshift trigger must stay automatic, not discretionary.

**RR-12 — Reviewer's top-20 risks.** Mapped: #1→R-2, #5→R-3, #6→R-53, #16→R-6, #17→R-19, #3→R-1, #20→fixed via RR-8; the remainder correspond to existing register entries or to dispositions above. No genuinely novel register entry required; register likelihoods unchanged pending MVP data.

## 2. What the review restated vs. what it found

Several P0s are the repo's own gates reflected back (RR-1, RR-2, RR-11's premise) — fair as emphasis, not new information; a reviewer confirming the kill criteria are the right kill criteria is useful signal, cheaply bought. The two findings that changed documents materially: the **perimeter contradiction** (RR-6) and the **staging gap** (RR-3), with RR-10's external-eval gate close behind. Score commentary: 58/100 for a pre-revenue docs-only company is roughly the score the repo assigns itself (`../investor/due-diligence.md` §3 answers "why $16M post" with milestones, not certainty); the number we're accountable to is the MVP acceptance table, not the review's rubric.

## 3. Applied-diff index (this pass)

`../product/prd.md` (FR-6 evidence band, story 5) · `../product/ux.md` (3 verdict→evidence-band edits) · `executive-summary.md` (tagline, hallucination-free, elevator) · `../investor/pitch-deck.md` (tagline) · `marketing.md` (trust pillar, banned absolutes) · `../roadmap/v1.md` (SMB metric gate, MCP deferral) · `../roadmap/mvp.md` (fallback map, gate 7 external red-team, share-rate success signal) · `financial-model.md` (deep-bear row) · `../architecture/ai-architecture.md` (as-of stamps, §13.6) · `../architecture/observability.md` (per-claim-class/source cost breakout) · `../architecture/system-design.md` (§10 build staging) · this file.

## 4. Second review (minty hermes, 2026-07-06, M-series) — dispositions

Received initially truncated (M4 + partial M5); the full M-series plus H/L findings arrived subsequently — dispositions for the complete review are in §5.

**RR-13 (M4) — Financial numbers need a source-of-truth sweep.** **ACCEPTED — verified, and worse than reported.** `business-plan.md` §4 didn't just use a "simplified LTV formula": its perpetuity-form LTV row ($13–19K prosumer / $150K team / $600K+ ent) contradicted the model's 36-month-capped methodology ($4.6K / $29K / $118K) by 3–5×, **and contradicted its own LTV/CAC row in the same table** (its stated LTVs over its stated CACs imply 12–17×, not the printed 3.5–4.5). Payback rows also diverged (bp 8–10 mo Team vs model ~5), churn start-point differed (2.4% vs canonical 2.6%), the model's "cohort DCF" was misdescribed, and the COGS owner was a CFO who doesn't exist until FY3. Fixed: `business-plan.md` §4 rewritten on the canonical methodology with reconciled FY2-basis figures; the perpetuity form is now **banned shorthand**; "$350" is legal only labeled as self-serve *channel* CAC. `financial-model.md` §12 expanded into the requested **Canonical Numbers Registry** (C1–C13: value / allowed shorthand / where used / owner / updated), with the maintenance rule that any number appearing in ≥2 docs gets a row before the second doc merges. One registry, in the model — a separate numbers-canon file was considered and rejected as a second source of truth waiting to drift.

**RR-14 (M5) — Marketplace + badge perimeter risk.** **PARTIAL (finding received truncated).** The visible concern is right: compliance §6.4 fenced the broker-dealer edge (analysis time, not outcomes) but said nothing about the **badge** — and a paid "Verified by DealPilot" mark on listings is an endorsement/advertising surface, not just a rake question. Added `../legal/compliance.md` §6 item 5: badges attest *process completion under a named rubric version* only; license terms prohibit endorsed/recommended phrasing; signed technical attestation required (register R-51); and **who paid for a verification is a disclosed provenance fact** on the memo/badge — the seller-capture guard from `business-plan.md` made visible rather than internal. Counsel opinion on endorsement liability is now a badge-GA pre-condition alongside the existing broker-dealer review. Marketplace itself stays Phase-3, board-gated, counsel-gated (unchanged — that part was already right). Awaiting M5's recommended fix and M1–M3 before closing the M-series.

**Diff index (this pass):** `business-plan.md` (§4 rewrite, COGS sentence, owner fix, badge pointer) · `financial-model.md` (§12 → canon registry C1–C13) · `../legal/compliance.md` (§6 item 5) · this file.

## 5. Full second review (minty hermes, complete H/M/L series) — dispositions

Verdict received: **PARTIAL PASS — not closed.** Correct verdict; §1–§3 of this file overstated closure in three places (H1, H2, H3 below prove it), and the status line has been downgraded accordingly (L1). Dispositions:

**H1 — "auditable recommendation" residue.** **ACCEPTED — reviewer right, our sweep was incomplete.** `../product/prd.md` §1 and README both still said "auditable recommendation(s)"; fixed to "auditable evidence memo(s)." Full sweep run on recommend/decisioning/worth-buying/should-acquire: `executive-summary.md` "embed verified decisioning" → "embed claim verification" (×2); remaining hits classified safe — `../legal/tos.md` §2.1 and `../legal/compliance.md` §6.1 are the bans themselves; `market-research.md`/`../product/personas.md` "human decisioning retained/preserved" describes humans deciding, which is the perimeter's whole point.

**H2 — MCP contradiction repo-wide.** **ACCEPTED — the deferral was declared, not enforced.** Fixed in all four cited locations: `../product/roadmap.md` 12-mo line (deferral noted), `marketing.md` DevRel (MCP held until post-A trust gates), `../engineering/api.md` §8 (now opens with **Status: designed, not shipped — deferred to post-Series-A** plus the M6 three-row API-alpha/API-GA/MCP policy table), `../investor/pitch-deck.md` S9 (MCP footnoted post-A). The design text in api.md §8 is deliberately retained under the status banner — agent-integration questions get an answer without the artifact becoming launch collateral.

**H3 — Fallback map was a sentence wearing a fake mustache.** **ACCEPTED — correct and fair.** The actual matrix now exists: `../architecture/evidence-fallback-map.md` — all MVP claim types × the reviewer's requested fields (materiality, tier for V/C, primary/backup source, manual path, validator, latency, cost, fail state, unblock text), plus the coverage rules the map forced into the open: lien_status and flood_zone have **no second machine source by nature** (the recorder and FEMA *are* the authority — resilience is the manual path + adapter alerting, stated not hidden), and occupancy/HOA have honest REPORTED ceilings. Cost column reconciles to the §10 COGS evidence-fee line. RR-1's §1 entry stands as history; this closes it.

**H4 — Risk-ID drift.** **ACCEPTED — verified: business-plan R-3/R-4/R-9 meant different risks than register #3/#4/#9.** `business-plan.md` §10 now uses canonical register row numbers only (header note: one scheme repo-wide), with #4b† marking the data-incumbent variant folded into #4's review. Register numbering is canonical everywhere; the C-registry (§12) and this file already used it.

**H5 — Pitch-deck absolutes.** **ACCEPTED.** "We never invent facts" → "Unsupported facts are blocked or marked UNKNOWN by design"; "forgeable by no one" → "tampering independently detectable" (the reviewer's distinction is exactly right — tamper-*evident* is the claim the architecture supports; key compromise, verifier bugs, and insiders exist, per our own `../architecture/threat-model.md` T-3/T-8); "zero rigor" → "no enforceable evidence contract."

**M1 — VERIFIED trigger too loose.** **ACCEPTED.** RR-8's trigger is now materiality-weighted (any blocking false-VERIFIED in red-team → RCA + launch-gate review; non-blocking >0.5% → mitigation plan; aggregate >1% or VI-incident → rename RFC), and MVP gate 7 adds **zero false VERIFIED on blocking claims** explicitly.

**M2 — Data-rights matrix.** **ACCEPTED as open legal artifact** — reclassified from "partial/specified" to **OPEN**: the reviewer's four-class matrix (contains Customer Content / cross-tenant reusable / opt-in / DPA clause) is adopted as the counsel-pass deliverable spec; DPA §-references stay TBD until that pass. Technical boundaries unchanged (`../architecture/ai-architecture.md` §8, `../legal/privacy.md` §3/§9).

**M3 — "Decision memo" definition.** **ACCEPTED.** Canonical definition added at `../product/prd.md` FR-6: *an evidence memo used to support a human transaction decision; describes claim support, contradictions, unknowns, and valuation assumptions; does not recommend whether to transact.* All docs inherit it.

**M4 / M5.** Dispositioned in §4 (RR-13/RR-14) before the full text arrived; the complete M5 fix list is now covered: counsel re-review ✓, no success fees ✓, seller-payment provenance disclosure ✓, **expert conflict disclosure added** (compliance §6 item 5), badge-not-payment-linked ✓, and marketplace marked **non-load-bearing** in `executive-summary.md` Phase 3 (was previously only in due-diligence).

**M6 — API/MCP terminology.** **ACCEPTED** via the three-row policy table in `../engineering/api.md` §8 (API alpha = v1/design partners/limited; API GA = Phase 3/commercial; MCP = post-A/thin client).

**L1** status downgraded (header). **L2** — fair; §0's "reviewer accuracy: high" refers to evidence substantiation, and per-finding dispositions carry the severity/fix judgments; noted. **L3** — **ACCEPTED, reviewer right to push**: register row **#101 Cross-doc doctrine drift** added (score 12, LEG) with its mitigations being exactly the machinery these passes built: canonical registries, the disposition process, and the new **`/tools/lint-docs.sh`** (banned-phrase + advice-verb + cross-reference lint, CI-runnable — the reviewer's "nice to fix" grep/CI item, implemented). **L4** — **ACCEPTED**: "procurement requirement we define" now marked as thesis in `executive-summary.md`.

**Open follow-ups (the reason the status isn't "Closed"):** DPA data-rights language (counsel), counsel pass on ToS/privacy finals, external red-team execution (MVP gate 7 — can only happen with a running system), badge-GA counsel opinion (Phase 3), quarterly re-rank absorbing register #101.

**Diff index (this pass):** `../product/prd.md` (§1, FR-6 definition) · README · `executive-summary.md` (×4) · `../product/roadmap.md` · `marketing.md` · `../investor/pitch-deck.md` (×4) · `../engineering/api.md` (§8 status + policy table) · `business-plan.md` (risk IDs) · `risk-register.md` (#101) · `../roadmap/mvp.md` (gate 7 blocking rule, map pointer) · **new:** `../architecture/evidence-fallback-map.md`, `/tools/lint-docs.sh` · this file.
